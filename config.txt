# Instancja TSP
instancja = instances/pcb442.tsp
OPT = 3323      # znany OPT
powtorzenia = 1

# Parametry ACO
alfa    = 1.0   # wplyw feromonu
beta    = 2.0   # wplyw heurystyki <2-5>
rho     = 0.2   # wspolcz. parowania <0,1]
Q       = 1.0   # stala wzmocnienia
m       = 0     # liczba mrowek (0 = m=n)
wariant = CAS   # CAS, DAS lub QAS
tau0_mode = nn  # nn: tau0=m/C_nn (RNN)
                # manual: wartosc ponizej
tau0    = 0.01  # aktywne gdy manual

# Warunki stopu
max_czas        = 900  # maks. czas w s
max_bez_poprawy = 300  # cykli bez poprawy

# Flagi sterujace
zapis_historii = false  # zapis f(x)(t)